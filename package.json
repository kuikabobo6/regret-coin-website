{
  "name": "regret-airdrop",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel start",
    "deploy": "vercel --prod",
    "logs": "vercel logs"
  },
  "dependencies": {
    "@vercel/postgres": "^0.5.0"
  },
  "devDependencies": {
    "@vercel/node": "^2.0.0"
  }
}